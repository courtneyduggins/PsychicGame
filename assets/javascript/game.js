// define variable for user guess, computer's choice, wins, losses &
// and number of guess left

// set wins and loss equal to starting count, 0
// set guesses Left equal to starting number, 9

var userGuess = event.key;
var computerChoice
var wins = 0;
var losses = 0;
var guessesLeft  = 9;

// when user presses key, determine if key pressed matches computer's choice



// if user guess matches computer choice, increase wins